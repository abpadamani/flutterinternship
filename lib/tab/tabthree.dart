import 'package:flutter/material.dart';

class Mythreetab extends StatelessWidget {
  const Mythreetab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Center(
        child: Text("Settings"),
      ),
    );
  }
}
