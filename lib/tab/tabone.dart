import 'package:flutter/material.dart';

class Myonetab extends StatelessWidget {
  const Myonetab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.cyan,
      child: Center(
        child: Text("Dashboard"),
      ),
    );
  }
}
