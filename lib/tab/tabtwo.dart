import 'package:flutter/material.dart';

class Mytwotab extends StatelessWidget {
  const Mytwotab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(
        child: Text("Profile"),
      ),
    );
  }
}
